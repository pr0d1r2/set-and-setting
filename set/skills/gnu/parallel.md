# GNU parallel

Use `parallel` from the dev shell (nixpkgs `parallel`). macOS
has no built-in equivalent. GNU parallel enables running shell
commands in parallel with job control, progress tracking, and
proper signal handling.
