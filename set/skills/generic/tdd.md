# TDD

We work in Test Driven Development scheme.
Ensure every implementation is covered with 1-to-1 unit test.
For every unit coverage create test file first and run it to confirm
RED and commit.
Then satisfy the flow with iterating over implementation; make commit
for each iteration step.
Make separate commit if spec has to be adjusted.
