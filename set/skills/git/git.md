# Git

Auto commit after successful prompt.
Separate topics in commits.
In case multiple refactors by model happening on the road -- we want
separate commit for each of them.
We want to observe decisions made in individual commits for faster
review.
Separate smaller commits will also minimize probability of making
mistakes.
Think about committing as a way to continuously offload context, so
piece of code at hand is less complicated and we do less mistakes.
