# Just

We use just to wrap up all commands to have navigable CLI.
We should have default action first that list all commands with
namespace, so we can copy-paste fast.
All other actions should be ordered alphabetically.
