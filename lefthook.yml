# Bare guardrail bootstrapped by hallucinogen (V239). Runs the flake's own
# checks so the merge gate (V74) is meaningful; the loop's migrate carrier
# (V190/V219) upgrades this to the referenced standard on a later pass.
pre-commit:
  commands:
    flake-check:
      run: nix flake check
