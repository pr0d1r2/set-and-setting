---
# Override timeouts for --all-files CI runs.

pre-commit:
  commands:
    narrow-language-other:
      timeout: 300s
    markdownlint:
      timeout: 300s
    markdownlint-agentic:
      timeout: 300s
