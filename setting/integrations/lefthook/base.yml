# gitleaks, git-conflict-markers, git-no-local-paths, execute-permissions,
# file-size-check (#101), trailing-whitespace, missing-final-newline,
# editorconfig-checker (#98), and typos (#100) are intentionally absent here:
# they are delivered as PINNED flake `checks.<sys>.<tool>` (set-and-setting.lib.
# mk{Gitleaks,GitConflictMarkers,GitNoLocalPaths,ExecutePermissions,
# FileSizeCheck,TrailingWhitespace,MissingFinalNewline,EditorconfigChecker,
# Typos}Check), replacing their runtime `remotes:` git_urls with offline-
# runnable, cache-warm checks. No base remotes remain (#93 strangler-fig
# complete for this fragment).
---
