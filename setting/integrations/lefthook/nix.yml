# All nix linters (flake-manifest, nixfmt, statix, deadnix, nix-no-embedded-shell)
# are delivered as PINNED flake `checks.<sys>.<tool>`
# (#97 nixfmt, #99 the rest), replacing their runtime `remotes:` git_urls
# with offline-runnable, cache-warm checks. No nix remotes remain.
---
