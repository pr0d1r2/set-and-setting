---
pre-commit:
  commands:
    rekall-check:
      glob: "{set/skills/**,.rekall/**}"
      run: >
        if command -v rekall >/dev/null 2>&1; then rekall check;
        else echo "lefthook SKIPPED rekall check -- not on PATH"; fi
    rekall-gnu-sed:
      glob: "*.{sh,bash,nix,yml,yaml}"
      run: sh .rekall/rules/always-use-sed-from-the-dev.sh
    set-skill-extension:
      glob: "set/{skills,drafts}/**"
      run: SET_ROOT=set bash lib/skill-extension-check.sh
    set-skill-size:
      glob: "set/{skills,drafts}/**/*.md"
      run: SET_ROOT=set bash lib/skill-size-check.sh
    set-ref-resolution:
      glob: "set/**/*.md"
      run: SET_ROOT=set REF_MATCH=lib/ref-match.sh bash lib/ref-resolve-check.sh
    set-bundle-content:
      glob: "set/**/*.md"
      run: SET_ROOT=set REF_MATCH=lib/ref-match.sh bash lib/bundle-content-check.sh

pre-push:
  commands:
    rekall-check:
      glob: "{set/skills/**,.rekall/**}"
      run: >
        if command -v rekall >/dev/null 2>&1; then rekall check;
        else echo "lefthook SKIPPED rekall check -- not on PATH"; fi
    rekall-gnu-sed:
      glob: "*.{sh,bash,nix,yml,yaml}"
      run: sh .rekall/rules/always-use-sed-from-the-dev.sh
    set-skill-extension:
      glob: "set/{skills,drafts}/**"
      run: SET_ROOT=set bash lib/skill-extension-check.sh
    set-skill-size:
      glob: "set/{skills,drafts}/**/*.md"
      run: SET_ROOT=set bash lib/skill-size-check.sh
    set-ref-resolution:
      glob: "set/**/*.md"
      run: SET_ROOT=set REF_MATCH=lib/ref-match.sh bash lib/ref-resolve-check.sh
    set-bundle-content:
      glob: "set/**/*.md"
      run: SET_ROOT=set REF_MATCH=lib/ref-match.sh bash lib/bundle-content-check.sh
