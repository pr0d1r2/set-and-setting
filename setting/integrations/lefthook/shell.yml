# shellcheck, no-shell-functions, and shfmt are intentionally absent here:
# they are delivered as PINNED flake `checks.<sys>.<tool>`
# (set-and-setting.lib.mk{Shellcheck,NoShellFunctions,Shfmt}Check / #98,
# #100), replacing the runtime `remotes:` git_urls with offline-runnable,
# cache-warm checks. No shell remotes remain.
---
