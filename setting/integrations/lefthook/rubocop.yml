---

pre-commit:
  commands:
    rubocop:
      glob: "**/*.rb"
      run: bundle exec rubocop --fail-fast --force-exclusion {staged_files}
      timeout: 60s

pre-push:
  commands:
    rubocop:
      glob: "**/*.rb"
      run: bundle exec rubocop --fail-fast
      timeout: 2m
